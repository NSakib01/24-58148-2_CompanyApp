# Required submission screenshots

Replace these placeholders with real screenshots captured from the completed
application in Visual Studio and SQL Server Object Explorer. Keep the exact
filenames below so the links in the repository README work automatically.

1. `01_unified_database.png` - `dbCompanyApp` expanded with `dbo.Users` and `dbo.Emp_details`.
2. `02_migrated_users.png` - SQL query results showing migrated `dbo.Users` accounts.
3. `03_solution_explorer.png` - the single project and nested `.cs`, `.Designer.cs`, and `.resx` form files.
4. `04_login.png` - the existing Login form with a valid username.
5. `05_registration.png` - the existing Registration form with matching passwords.
6. `06_dashboard.png` - the Dashboard showing the authenticated username.
7. `07_employee_creator.png` - the employee grid showing a real username in the `CreatedBy` column.
8. `08_logout.png` - the logout confirmation or cleared Login form after logout.
9. `09_bonus_search.png` - employee search results and/or the delete confirmation dialog.

Do not use fabricated screenshots. The final application must be run on Windows
with SQL Server LocalDB before these images can be captured.
